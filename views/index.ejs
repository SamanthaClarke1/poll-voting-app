<!DOCTYPE html>

<html>
	<body>
    <div class="mincon container text-center">
      <% if(user) { %> 
        <h1> Hello <%= user.displayName %>! </h1>
      <% } else { %>
        <h1> Not logged in. </h1>
      <% } %>
    </div>
	</body>

</html>